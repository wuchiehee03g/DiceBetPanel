{
  "name": "dicebetpanel",
  "version": "3.0.0",
  "private": true,
  "description": "骰俠吹牛王投注站 —— 靜態頁 + Firebase Realtime Database，無後端",
  "scripts": {
    "test": "node tests/syntaxcheck.js index.html banker.html && node tests/logic.test.js && node tests/dom.test.js",
    "test:e2e": "node tests/e2e.test.js",
    "deploy:rules": "firebase deploy --only database"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
